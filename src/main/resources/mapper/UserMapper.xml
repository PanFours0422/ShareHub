<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sharehub.mapper.UserMapper">
    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, username, password, email, create_time, update_time, status
    </sql>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.sharehub.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, status)
        VALUES (#{username}, #{password}, #{email}, #{status})
    </insert>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.sharehub.entity.User">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
        AND status = 1
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultType="com.sharehub.entity.User">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE email = #{email}
        AND status = 1
    </select>
</mapper> 